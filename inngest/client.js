import { Inngest } from "inngest";
export const inngest = new Inngest({
    id: "ticketing-system" });


// ✔ Basically: This code creates an Inngest client for your app and names it "ticketing-system" so you can define background functions and event triggers across your project.